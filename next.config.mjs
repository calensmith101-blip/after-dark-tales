/** @type {import('next').NextConfig} */
const nextConfig = {
  // 'output: export' has been removed — it disabled all API routes (including /api/generate-story)
  // Vercel will now run this as a standard Next.js serverless app
};

export default nextConfig;
